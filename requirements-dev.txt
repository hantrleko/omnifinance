# OmniFinance — development & CI dependencies.
# Install on top of requirements.txt for running tests, linting, and type
# checks locally or in CI.

-r requirements.txt

pytest>=7.4.0,<9.0.0
pytest-cov>=4.1.0,<6.0.0
ruff>=0.4.0,<1.0.0
mypy>=1.8.0,<2.0.0
pandas-stubs>=2.0.0
types-requests>=2.31.0
