# OmniFinance — optional REST API layer (core/api.py).
# Install on top of requirements.txt when exposing the FastAPI endpoints,
# e.g. `uvicorn core.api:app --host 0.0.0.0 --port 8000`.

-r requirements.txt

fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
pydantic>=2.5.0,<3.0.0
